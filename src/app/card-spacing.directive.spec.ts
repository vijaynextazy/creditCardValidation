import { CardSpacingDirective } from './card-spacing.directive';

describe('CardSpacingDirective', () => {
  it('should create an instance', () => {
    const directive = new CardSpacingDirective();
    expect(directive).toBeTruthy();
  });
});
